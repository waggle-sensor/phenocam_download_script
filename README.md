# phenocam mirror script


In mirror mode the downloaded zip file is extracted into a mirror directory and then deleted. A zip file is downloaded only if the jpg-files do not exist in the mirror directory.
